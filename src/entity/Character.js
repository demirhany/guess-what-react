class Character {
    constructor(ch, isCorrect, isInWord, isNotInWord) {
        this.ch = ch;
        this.isCorrect = isCorrect;
        this.isInWord = isInWord;
        this.isNotInWord = isNotInWord;
    }
}

export default Character;